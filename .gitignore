# 시크릿 — 절대 커밋 금지
.env
secret/
*.env.local

# 데이터 (공용PC 로컬 저장분)
data/
process/
*.parquet
*.tmp
*.jsonl

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/

# 엑셀 임시파일
~$*.xlsx

# 전달용 압축 패키지 (내용은 이미 소스에 반영됨)
*.zip

# 개인 배포판 빌드 산출물 — build_standalone.py 로 언제든 재생성.
# 커밋하면 index.html 한 글자 고칠 때마다 130KB diff 가 붙고, 원본과 어긋난
# stale 산출물이 정본처럼 보일 위험이 있다.
dist/

# OS / 에디터
Thumbs.db
.DS_Store
.vscode/
.idea/
